/**
 * Test setup for {{repository}}
 * Generated by Mock-Driven Testing Revolution
 * 
 * This file sets up all necessary mocks for isolated testing
 */

// Import generated mocks
{{#if (eq mockStrategy "api-responses")}}
require('./api-mocks');
{{/if}}
{{#if (eq mockStrategy "service-stubs")}}
require('./service-stubs');
{{/if}}

// Global test configuration
global.testConfig = {
  repository: '{{repository}}',
  mockStrategy: '{{mockStrategy}}',
  timeout: 30000
};

// Setup test environment
beforeAll(() => {
  console.log('🧪 Setting up test environment for {{repository}}');
});

afterAll(() => {
  console.log('🧹 Cleaning up test environment');
});

// Helper functions
global.mockHelpers = {
  resetAllMocks: () => {
    jest.clearAllMocks();
  },
  
  getMockCallCount: (mockFn) => {
    return mockFn.mock.calls.length;
  },
  
  getLastMockCall: (mockFn) => {
    const calls = mockFn.mock.calls;
    return calls[calls.length - 1];
  }
};